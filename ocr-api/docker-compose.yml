services:
  ocr-api:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./output:/ocr-api/output
      - /etc/localtime:/etc/localtime:ro
      - ${RCLONE_CONFIG_DIR:-../rclone}:/ocr-api/.config/rclone
    env_file:
      - ../.env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
